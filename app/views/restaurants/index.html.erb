<div class="container">
<% @restaurants.each do |restaurant| %>
  <h2><%=restaurant.name%></h2>
  <%=link_to "See details", restaurant_path(restaurant), class: "btn btn-primary" %>
  <%= link_to 'Leave a review', new_restaurant_path(restaurant), class: "btn btn-primary" %>

<% end %>
</div>
